@model IEnumerable<Biblioteca.Web.Models.Clienti>

@{
    ViewBag.Title = "Index";
}


<p>
    @Html.ActionLink("Client nou", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nume)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adresa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contact)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nume)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Adresa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contact)
        </td>
        <td>
            @Html.ActionLink("Editeaza", "Edit", new { id=item.IdClient }) |
            @Html.ActionLink("Sterge", "Delete", new { id=item.IdClient })
        </td>
    </tr>
}

</table>
